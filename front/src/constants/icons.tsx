import { Add, Calendar, Save } from '@icon-park/react'

const IconsList = {
	Add: <Add theme={'filled'} size={24} />,
	Calender: <Calendar theme={'filled'} size={24} />,
	Save: <Save theme={'filled'} size={24} />
}

export default IconsList
